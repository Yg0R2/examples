version: "3.8"

networks:
  example-network:
    name: example-network
    driver: bridge

volumes:
  keystore-volume:
    driver: local
    name: keystore-volume
    driver_opts:
      device: /host_mnt/w/Projects/examples/nginx/cert/
      type: none
      o: bind
