dependencies {
    // Added because of the user client implementation is here
    compileOnly group: 'org.springframework.boot', name: 'spring-boot-starter-web'
    implementation group: 'yg0r2.examples', name: 'user-client', version: '+'
}
