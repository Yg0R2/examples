pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'user'

include 'api'
include 'dao'
include 'service'
include 'web'
